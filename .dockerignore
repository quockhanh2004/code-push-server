# Docker related
Dockerfile
.dockerignore

# Git related
.git
.gitignore

# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts (điều chỉnh nếu có các thư mục build khác)
# Ví dụ nếu npm run build tạo thư mục 'dist'
# dist

# Cache and temporary files
.cache
.tmp
temp

# Editor and OS generated files
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Process config (sẽ được copy riêng)
# process.json # Chỉ bỏ comment dòng này nếu bạn không muốn nó bị copy bởi 'COPY --from=builder /app .',
             # nhưng thường copy chung là ok vì nó nằm trong /app